import { TermsPageContent } from "@/components/pages/terms-page";

export const dynamic = "force-dynamic";

export default function TermsPageLocalized() {
  return <TermsPageContent locale="de" />;
}
